name: Deploy Status

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://sellify-multivendor-hub.onrender.com
    steps:
      - name: Deploy Confirmation
        run: |
          echo "Successfully deployed to Render!"
